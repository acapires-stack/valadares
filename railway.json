{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "server/**",
      "package.json",
      "package-lock.json",
      "Dockerfile",
      "railway.json"
    ]
  }
}
