// Preload bridge — runs no contexto isolado entre main e renderer.
// Atualmente Valadares não precisa de APIs nativas no cliente, então mantemos vazio.
// (Stub presente pra estrutura correta; expor APIs no futuro se for usar arquivo local,
// notificações nativas, etc.)
